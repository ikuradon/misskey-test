deploy:
  # - okteto build -t okteto.dev/misskey:latest --build-arg NODE_ENV=development
  - echo helm upgrade --install misskey chart --set url="https://$(kubectl config view --minify -o jsonpath='{..namespace}').cloud.okteto.net"
